import React from 'react';
import Layout from '../../Layout/Layout';

const Blogs = () => {
    return ( 
        <Layout>
            <h2>Blog's Page !</h2>
        </Layout>
    );
};

export default Blogs;